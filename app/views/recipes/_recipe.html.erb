<div id="<%= dom_id recipe %>" class="d-flex flex-column gap-3">
  <h1 class="text-center">
    <%= recipe.name %>
  </h1>

  <div class="container-lg row">

    <div class="col">
      <div>
        <strong>Preparation time:</strong>
        <%= recipe.preparation_time.to_s(:time) %>
      </div>

      <div>
        <strong>Cooking time:</strong>
        <%= recipe.cooking_time.to_s(:time) %>
      </div>
    </div>
    
    <div class="col d-flex justify-content-end">
      <div class="form-check form-switch">
        <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDisabled" <%= 'checked' if recipe.public %> disabled>
        <label class="form-check-label" for="flexSwitchCheckDisabled">Public</label>
      </div>
    </div>
  </div>

  <div class="container-lg row">
    <h4>Description:</h4>
    <div><%= recipe.description %><div>
  </div>

  <div class="d-flex justify-content-between container-lg mt-3 mb-3">
    <%= link_to 'Generate Shopping List', recipe_shopping_list_path(recipe), class: 'btn btn-secondary' %>
    <%= link_to 'Add ingredient', new_recipe_recipe_food_path(recipe), class: 'btn btn-success' %>
  </div>
  <%= render 'shared/food_table' do %>
    <%= render @recipe_foods %>
  <% end %>
</div>
